import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  messageContainer: {
      padding: 10,
      marginBottom: 10,
      borderRadius: 15
  },
  message: {
    
  },
  createdAt: {
    fontSize: 10,
    marginTop: 5,
    alignSelf: 'flex-end',
    color: 'grey'
  }
});

export default styles;
