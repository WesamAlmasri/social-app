import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  feedContainer: {
      width: '100%'
  },
  noPostText: {
    alignSelf: 'center',
    marginTop: 20,
  }
});

export default styles;
