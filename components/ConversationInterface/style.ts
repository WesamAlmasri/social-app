import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  feedContainer: {
      width: '100%',
      flex: 1,
      padding: 15
  },
  noChatText: {
    fontSize: 20
  }
});

export default styles;
