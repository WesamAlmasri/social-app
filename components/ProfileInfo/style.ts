import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    width: '100%',
    padding: 10,
    borderTopWidth: 1,
    backgroundColor: '#fff',
    borderColor: '#e5e5e5',
    marginBottom: 10
  },
  profileInfoRow: {
    flexDirection: 'row',
    alignItems: 'center',
    marginBottom: 10
  },
  profileInfoText: {
    color: '#949494',
    marginLeft: 10
  }
});

export default styles;
